Place the institutional logo in this directory as
`LOGO-MODIFICADO-SUPERIOR-izq-1.png`.

The Tk dashboard loads it through a project-relative path and preserves its
aspect ratio. The interface uses a text fallback when the resource is absent.
