# ADR 003: InferenceBackend Plugin Contract

Status: Accepted. Plugins use `InferenceBackend`, structured capabilities,
descriptors, API versions and deterministic priority. External plugins are trusted.
